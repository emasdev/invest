require 'rails_helper'

RSpec.describe Investment, type: :model do

  describe 'associations' do
    # it { should belongs_to(:author).class_name("Users") }
    # it { should have_and_belongs_to_many(:groups) }
  end
end
